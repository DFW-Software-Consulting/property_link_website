import { ListingLoadingSkeleton } from "@/components/marketing/listing-loading-skeleton";

/** Skeleton shown while the short-term stays listing fetches CMS data. */
export default function ShortTermStaysLoading() {
  return <ListingLoadingSkeleton />;
}
